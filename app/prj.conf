# Abilita gli assert del kernel: serviranno a rilevare i parametri corrotti
# nella fase di iniezione (failure mode "assert-panic"). In una run pulito non scattano.

CONFIG_ASSERT=n


# gli scenari sovrascriveranno queste macro
CONFIG_FI_ENABLE=n
CONFIG_FI_VALUE=0x40
CONFIG_FI_TRIGGER=1

